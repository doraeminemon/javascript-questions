// 1) Create Memoization Function
  // DEMONSTRATES: understanding of closure and functions as first class citezens

  // IMPLEMENT function `memoize` to make below work.
    // Tip: `memoize` should be implemented so that it knows nothing about the function it is memoizing
    // Bonus: make memoized value only valid for 5 seconds.

    // Example:  
    //    const memoizedRecursiveFibonacci = memoize(recursiveFibonacci, 2000);


module.exports = function memoize(func, cacheValidityInSeconds_optional_bonus) {
  // your code here
};
